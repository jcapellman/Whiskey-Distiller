<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:WhiskeyDistiller.library.Views"
             xmlns:vm="using:WhiskeyDistiller.library.ViewModels;assembly=WhiskeyDistiller.library"
             x:Class="WhiskeyDistiller.library.Views.MainPage">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Text="Whiskey Distiller" />

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Button Grid.Row="0" Command="{Binding NewGameCommand}" Text="New Game" />
            <Button Grid.Row="1" Text="Load Game" />
            <Button Grid.Row="2" Text="Options" />
            <Button Grid.Row="3" Text="About" />
        </Grid>
    </Grid>
</ContentPage>